import { AccessTokenDirective } from './cms-access-token.directive';

describe('AccessTokenDirective', () => {
  it('should create an instance', () => {
    const directive = new AccessTokenDirective();
    expect(directive).toBeTruthy();
  });
});
